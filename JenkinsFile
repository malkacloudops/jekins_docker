pipeline {
    agent any

    stages {
        stage('Hello') {
            steps {
                powershell 'Write-Output "First time running powershell on Jenkins"'
            }
        }
    }
}